#include <utility>      
#include <iostream>   
#include <vector>
#include <cstdlib>
#include "nodo.hpp"


using namespace std ;

template <class T, class N>
class tree {
			
		public:
		vector<nodo> nodevect;
		~tree ();
		void tree(nodo root);
};
  void tree(nodo root){
	 
	 	 
	 }
